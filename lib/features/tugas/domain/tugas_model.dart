class TugasModel {
  int id;
  int makul;
  String judul;
  String deskripsi;
  String deadline;
  int difference;

  TugasModel(this.id, this.makul, this.judul, this.deskripsi, this.deadline,
      this.difference);
}
